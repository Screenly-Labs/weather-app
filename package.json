{
  "name": "screenly-weather-app",
  "private": true,
  "version": "0.0.1",
  "main": "src/index.jsx",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "sync-fonts": "bun run sync-fonts.js",
    "dev": "wrangler dev",
    "build": "bun run build.js",
    "deploy": "wrangler deploy --env=dev"
  },
  "license": "MIT",
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.2.9",
    "@fontsource-variable/hanken-grotesk": "^5.2.8",
    "hono": "^2.7.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.19.0",
    "wrangler": "^4.104.0"
  },
  "ts-standard": {
    "project": "tsconfig.json"
  }
}
